[project]
name = "infra-guard"
version = "0.1.0"
description = "Linux infrastructure log monitor and safe auto-response tool"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
infra-guard = "infra_guard.cli:main"

[tool.pytest.ini_options]
pythonpath = ["."]
